name: XENOMORPH GUT Capability Admission Test

on:
  push:
    branches: [ main ]
    paths:
      - 'nostromo/gut/capability-admission.mjs'
      - 'nostromo/integration/gut-capability-admission-test.mjs'
      - 'nostromo/integration/gut-capability-admission-empty-metadata-test.mjs'
      - '.github/workflows/nostromo-gut-capability-admission-test.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    if: ${{ !contains(github.event.head_commit.message, '[nostromo-test]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify guarded foreign-capability admission
        run: |
          node nostromo/integration/gut-capability-admission-test.mjs
          node nostromo/integration/gut-capability-admission-empty-metadata-test.mjs
      - name: Commit focused result
        if: always()
        run: |
          git config user.name "nostromo-bot"
          git config user.email "actions@users.noreply.github.com"
          test -f nostromo/integration/gut-capability-admission-last-result.json && cp nostromo/integration/gut-capability-admission-last-result.json /tmp/gut-capability-admission-last-result.json || true
          git fetch origin main
          git reset --hard origin/main
          if [ -f /tmp/gut-capability-admission-last-result.json ]; then
            cp /tmp/gut-capability-admission-last-result.json nostromo/integration/gut-capability-admission-last-result.json
            git add nostromo/integration/gut-capability-admission-last-result.json
          fi
          if git diff --cached --quiet; then
            echo "No result change"
          else
            git commit -m "[nostromo-test] record GUT capability admission result"
            git push origin HEAD:main
          fi
