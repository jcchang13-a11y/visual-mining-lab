name: NOSTROMO GUT Date Thickening

on:
  push:
    branches: [ main ]
    paths:
      - '.nostromo-triggers/gut-date.json'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  thicken:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply GUT Date preservation patch
        run: node nostromo/integration/apply-gut-date-thickening.mjs
      - name: Verify focused Date adversarial case
        run: node nostromo/integration/gut-date-test.mjs
      - name: Verify existing GUT metabolism regression
        run: node nostromo/integration/gut-metabolism-test.mjs
      - name: Verify cross-organ integration regression
        run: node nostromo/integration/ci-test.mjs
      - name: Commit verified thickening
        run: |
          git config user.name "nostromo-bot"
          git config user.email "actions@users.noreply.github.com"
          git add nostromo/gut/gut-engine.js nostromo/integration/gut-metabolism-test.mjs nostromo/integration/gut-date-last-result.json nostromo/integration/gut-metabolism-last-result.json nostromo/integration/ci-last-result.json
          git commit -m "NOSTROMO: apply verified GUT v0.2.31 Date preservation"
          git pull --rebase origin main
          git push origin HEAD:main
