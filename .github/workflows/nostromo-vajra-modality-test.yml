name: NOSTROMO VAJRA Modality Test

on:
  push:
    branches: [ main ]
    paths:
      - 'nostromo/integration/vajra-ambiguity-guard.mjs'
      - 'nostromo/integration/vajra-modality-test.mjs'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  test:
    if: ${{ !contains(github.event.head_commit.message, '[nostromo-test]') }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Verify VAJRA bounded modality false-certainty guard
        run: node nostromo/integration/vajra-modality-test.mjs
      - name: Commit modality test result
        if: always()
        run: |
          git config user.name "nostromo-bot"
          git config user.email "actions@users.noreply.github.com"
          test -f nostromo/integration/vajra-modality-last-result.json && cp nostromo/integration/vajra-modality-last-result.json /tmp/nostromo-vajra-modality-last-result.json || true
          git fetch origin main
          git reset --hard origin/main
          if [ -f /tmp/nostromo-vajra-modality-last-result.json ]; then
            cp /tmp/nostromo-vajra-modality-last-result.json nostromo/integration/vajra-modality-last-result.json
            git add nostromo/integration/vajra-modality-last-result.json
          fi
          if git diff --cached --quiet; then
            echo "No result change"
          else
            git commit -m "[nostromo-test] record VAJRA modality result"
            git push origin HEAD:main
          fi
