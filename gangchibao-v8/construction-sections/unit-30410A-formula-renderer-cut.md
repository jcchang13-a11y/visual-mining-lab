# 30410A｜公式 renderer 改線斷面

> 狀態：保留施工痕跡，不回滾正文，不把舊式重新指定為正式規格。

## 位置

單元：`unit-30410A.md`｜十八分〈一體同觀分｜老師不是地球人〉。

這一段很適合留下，因為它同時把「空間／時間／眾生心」推到超維尺度，公式本身也剛好在施工途中換過管線。

## 初始封存斷面

在 V8 首次封存節點：

`fa5fe86379f3ef74934b4160f8fd2f0f68e44174`

PLS 使用的是舊工作語言：

`心進入「S / S = S barred」`

正文接著直接寫：

`這裡的「心」進入「S / S = S barred」公式：「心」／心 = 心 barred。`

也就是說，當時「心」的拆解仍靠 `S barred` 這套施工記號直接裸露在正文裡。

## 自動標準化改線

`049bd81d5f4bc8fdcedc0b979cb71329bd84f93a`

提交訊息：`Normalize V8 formula syntax`

這次把本單元的兩個裸寫公式入口換成 renderer 接口：

- `S / S = S barred` → `{{S}}`
- `「心」／心 = 心 barred` → `{{心}}`

現行正文因此成為：

`心進入「{{S}}」`

以及：

`這裡的「心」進入「{{S}}」公式：{{心}}。`

## 現行正式規格

第八版目前正式公式規格仍是：

`S = ～S`

名稱層次則使用上層 `S`／下層 `～S` 的直式公式。

所以這裡不是「舊式錯、新式對」的排版修正，而是至少三層施工同時存在：

1. 裸露工作語言：`S / S = S barred`
2. renderer 接口：`{{S}}`／`{{心}}`
3. 現行正式規格：`S = ～S`

## 暫時判定

不把 `unit-30410A.md` 改回舊式，也不為了整齊把這段歷史抹掉。

這個單元的內容本來就在拆「心」能不能成為最後可安住的位置；公式介面本身也沒有一個永遠不動的位置，反而形成很準的施工呼應。舊字形、renderer、正式規格三層並置即可。

**施工狀態：接口已換線，舊管線收入剖面；不封牆。**
