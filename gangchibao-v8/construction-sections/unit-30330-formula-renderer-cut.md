# 30330 施工剖面｜世界／微塵公式換線

> 狀態：保留，不修順。
>
> 這不是正文的替代稿，而是第八版「爛尾樓版」的施工斷面。它記錄同一段文字從裸寫工作公式換成 renderer 接口後，舊的工作語言仍留在牆外。

## A｜初始封存斷面

基準：`fa5fe86379f3ef74934b4160f8fd2f0f68e44174`

第十三分後段「比大小：世界與微塵」原本直接寫：

```text
"微塵" / 微塵 = 微塵 barred

"世界" / 世界 = 世界 barred
```

這是公式視覺語法尚未統一以前的裸寫工作管線。名字在上、被劃掉／barred 的概念在下，但還沒有接入後來的公式 renderer。

## B｜目前 main 的接口

目前正文相同位置改成：

```text
{{微塵}}

{{世界}}
```

也就是公式本體不再直接寫死在 Markdown 裡，而交給 reader 的 renderer 生成。正式規格後來確立為 `S = ～S` 與名稱層次直式公式。

## C｜沒有一起換掉的舊施工語言

接口換了，但正文前一句仍寫：

> 用我們現在比較穩的純文字公式，可以寫成：

這句現在已經不完全是真的：後面不是「純文字公式」，而是 renderer 呼叫。

正常出版整理會把這句修成「可寫成」或直接刪掉「純文字」；爛尾樓版不做這件事。這個錯位保留，因為它清楚顯示：

```text
工作公式（barred）
        ↓
renderer 接口 {{S}}
        ↓
正式規格 S = ～S
```

三層不是同一時間完成的。介面已換線，解說文字還卡在上一個施工階段。

## D｜本斷面的判定

- 正式規格：仍以 `S = ～S` 與名稱層次直式公式為準。
- `barred`：只保留施工史身份，不重新取得正式規格地位。
- `{{微塵}}`／`{{世界}}`：保留現行 renderer 接口。
- 「純文字公式」：暫不修，標記為裸露舊管線。
- 不把這次變更描述成單純排版修正；它是公式視覺語法的改線。

這個斷面因此不是「錯誤待修」，而是第八版需要看得見的鋼筋。