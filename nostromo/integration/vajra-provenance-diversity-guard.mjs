const clean=v=>String(v??'').normalize('NFKC').replace(/\s+/g,' ').trim();
const REQUIRED=['DROPLET','MUTHER','SHROOMING'];

function canonicalProvenance(v){return clean(v).toLowerCase().replace(/[\p{P}\p{S}\s]+/gu,'');}
function protectSemanticOperators(v){let s=clean(v).toLowerCase();const replacements=[[/!=/g,' op_ne '],[/>=/g,' op_gte '],[/<=/g,' op_lte '],[/==/g,' op_eqeq '],[/(?<=[\p{L}\p{N})])\s*>\s*(?=[\p{L}\p{N}(])/gu,' op_gt '],[/(?<=[\p{L}\p{N})])\s*<\s*(?=[\p{L}\p{N}(])/gu,' op_lt '],[/(?<=[\p{L}\p{N})])\s*=\s*(?=[\p{L}\p{N}(])/gu,' op_eq '],[/(?<=[\p{L}\p{N})])\s*\+\s*(?=[\p{L}\p{N}(])/gu,' op_plus '],[/(?<=[\p{L}\p{N})])\s*\*\s*(?=[\p{L}\p{N}(])/gu,' op_mul '],[/(?<=[\p{L}\p{N})])\s*\/\s*(?=[\p{L}\p{N}(])/gu,' op_div '],[/(?<=[\p{L}\p{N})])\s*[×·]\s*(?=[\p{L}\p{N}(])/gu,' op_mul '],[/(?<=[\p{L}\p{N})])\s*÷\s*(?=[\p{L}\p{N}(])/gu,' op_div ']];for(const [re,token] of replacements)s=s.replace(re,token);s=s.replace(/(?<=[\p{L}\p{N})])\s+-\s+(?=[\p{L}\p{N}(])/gu,' op_minus ');return s;}
function semanticOperatorSignature(v){const protectedText=protectSemanticOperators(v);return [...protectedText.matchAll(/\bop_(?:ne|gte|lte|eqeq|gt|lt|eq|plus|minus|mul|div)\b/g)].map(m=>m[0]).sort();}
function canonicalMaterial(v){return protectSemanticOperators(v).replace(/[\p{P}\p{S}\s]+/gu,'');}
function shingleSet(v,size=5){const s=canonicalMaterial(v),out=new Set();if(s.length<size)return out;for(let i=0;i<=s.length-size;i++)out.add(s.slice(i,i+size));return out;}
function jaccard(a,b){if(!a.size||!b.size)return 0;let intersection=0;for(const x of a)if(b.has(x))intersection++;return intersection/(a.size+b.size-intersection);}
function boundedNearDuplicate(a,b){const ca=canonicalMaterial(a),cb=canonicalMaterial(b);if(ca.length<48||cb.length<48)return {match:false,score:0,mode:'TOO_SHORT'};if(ca===cb)return {match:true,score:1,mode:'CANONICAL_EXACT'};const opA=semanticOperatorSignature(a),opB=semanticOperatorSignature(b);if(opA.length&&opB.length&&opA.join('|')!==opB.join('|'))return {match:false,score:0,mode:'SEMANTIC_OPERATOR_DIVERGENCE',operatorSignatureA:opA,operatorSignatureB:opB};const shorter=ca.length<=cb.length?ca:cb,longer=ca.length>cb.length?ca:cb;const containment=longer.includes(shorter)?shorter.length/longer.length:0,grams=jaccard(shingleSet(ca),shingleSet(cb)),score=Math.max(containment,grams);return {match:score>=0.82,score:Number(score.toFixed(4)),mode:containment>=grams?'BOUNDED_CONTAINMENT':'CHAR_5GRAM_JACCARD'};}
function returnsFromBranch(branch){const ctx=branch?.adjudicationContext;if(!ctx?.byOrgan)return [];return REQUIRED.map(o=>ctx.byOrgan[o]).filter(Boolean);}
function groupCrossOrgan(rows,keyName,minLength=1){const grouped=new Map();for(const r of rows){const key=r[keyName];if(!key||key.length<minLength)continue;if(!grouped.has(key))grouped.set(key,[]);grouped.get(key).push(r);}return [...grouped.entries()].filter(([,rs])=>new Set(rs.map(r=>r.sourceOrgan)).size>1);}
function nearContentAudit(rows){const collisions=[];for(let i=0;i<rows.length;i++)for(let j=i+1;j<rows.length;j++){const a=rows[i],b=rows[j];if(a.sourceOrgan===b.sourceOrgan)continue;if(a.materialIdentity&&a.materialIdentity===b.materialIdentity)continue;const probe=boundedNearDuplicate(a.material,b.material);if(!probe.match)continue;collisions.push({organs:[a.sourceOrgan,b.sourceOrgan].sort(),provenanceLabels:[a.provenance,b.provenance],materialSamples:[a.material,b.material],similarityScore:probe.score,similarityMode:probe.mode,boundary:'Bounded lexical near-replay signal only; this does not prove semantic identity or common upstream origin.'});}return collisions;}
function provenanceAudit(returns){const normalized=(Array.isArray(returns)?returns:[]).filter(r=>REQUIRED.includes(clean(r?.sourceOrgan))).map(r=>({sourceOrgan:clean(r.sourceOrgan),provenance:clean(r.provenance),provenanceIdentity:canonicalProvenance(r.provenance),material:clean(r.material),materialIdentity:canonicalMaterial(r.material),relation:clean(r.relation),packetId:clean(r.packetId),returnKey:clean(r.returnKey)}));const provenanceGroups=groupCrossOrgan(normalized,'provenanceIdentity');const collisions=provenanceGroups.map(([provenanceIdentity,rows])=>({provenanceIdentity,provenanceLabels:[...new Set(rows.map(r=>r.provenance))].sort(),organs:[...new Set(rows.map(r=>r.sourceOrgan))].sort(),formatMutationDetected:new Set(rows.map(r=>r.provenance)).size>1}));const materialGroups=groupCrossOrgan(normalized,'materialIdentity',24);const contentCollisions=materialGroups.map(([materialIdentity,rows])=>({materialIdentity,materialSamples:[...new Set(rows.map(r=>r.material))].slice(0,4),organs:[...new Set(rows.map(r=>r.sourceOrgan))].sort(),provenanceLabels:[...new Set(rows.map(r=>r.provenance))].sort(),distinctCanonicalProvenanceCount:new Set(rows.map(r=>r.provenanceIdentity).filter(Boolean)).size,formatMutationDetected:new Set(rows.map(r=>r.material)).size>1}));const nearContentCollisions=nearContentAudit(normalized),receivedOrgans=[...new Set(normalized.map(r=>r.sourceOrgan))].sort(),uniqueProvenanceCount=new Set(normalized.map(r=>r.provenanceIdentity).filter(Boolean)).size,complete=REQUIRED.every(o=>receivedOrgans.includes(o)),sharedProvenance=collisions.length>0,sharedContent=contentCollisions.length>0,nearSharedContent=nearContentCollisions.length>0,formatMutationCollisions=collisions.filter(c=>c.formatMutationDetected).length,contentFormatMutationCollisions=contentCollisions.filter(c=>c.formatMutationDetected).length,replayRisk=sharedProvenance||sharedContent||nearSharedContent;return {requiredOrgans:[...REQUIRED],receivedOrgans,complete,receivedCount:normalized.length,uniqueProvenanceCount,collisions,contentCollisions,nearContentCollisions,sharedProvenance,sharedContent,nearSharedContent,replayRisk,formatMutationCollisions,contentFormatMutationCollisions,structurallyDistinctProvenanceLabels:complete&&!sharedProvenance&&uniqueProvenanceCount===receivedOrgans.length,sourceIndependenceProven:false,status:replayRisk?'UPSTREAM_REPLAY_REVIEW_REQUIRED':complete?'PROVENANCE_LABELS_DISTINCT_NOT_INDEPENDENCE_PROOF':'PARTIAL_PROVENANCE_COVERAGE',boundary:'This guard compares bounded canonical provenance labels, operator-preserving canonical returned material, and bounded lexical near-replay. Multiplication and division are now protected experimentally, but public capability version remains v0.3.3 until the new adversarial case is CI-verified. Distinct labels and material are not proof of source independence.'};}
export function guardVajraAdjudicationProvenance(vajra){const source=vajra&&typeof vajra==='object'?vajra:{unresolved:[]};let collisionBranches=0,auditedBranches=0,formatMutationCollisionBranches=0,contentCollisionBranches=0,contentFormatMutationCollisionBranches=0,nearContentCollisionBranches=0;const unresolved=(source.unresolved||[]).map(branch=>{const returns=returnsFromBranch(branch);if(!returns.length)return branch;auditedBranches++;const audit=provenanceAudit(returns);if(!audit.replayRisk)return {...branch,provenanceDiversityAudit:audit};collisionBranches++;if(audit.formatMutationCollisions>0)formatMutationCollisionBranches++;if(audit.sharedContent)contentCollisionBranches++;if(audit.nearSharedContent)nearContentCollisionBranches++;if(audit.contentFormatMutationCollisions>0)contentFormatMutationCollisionBranches++;const contentOnly=(audit.sharedContent||audit.nearSharedContent)&&!audit.sharedProvenance;return {...branch,provenanceDiversityAudit:audit,nextAction:contentOnly?'AUDIT_CROSS_ORGAN_CONTENT_REPLAY_BEFORE_REASSESSMENT':'AUDIT_SHARED_UPSTREAM_BEFORE_REASSESSMENT',nextQuestion:contentOnly?'多個器官雖使用不同 provenance 標記，卻回傳相同、僅表面格式不同，或高度重疊的實質材料；先確認是否只是同一上游內容被重新包裝，再決定是否需要真正不同來源的證據。':'多個器官的回傳具有相同 provenance 或重複／高度重疊的實質材料；先確認它們是否只是同一上游材料的不同加工版本，再決定是否需要補一個真正不同來源的證據。',closureBlocked:true,closureAuthority:'NONE',boundary:'Cross-organ replay risk changed VAJRA behavior; raw caller-supplied returns remain preserved.'};});return {...source,organ:'VAJRA',capability:'ADJUDICATION_PROVENANCE_CONTENT_AND_NEAR_REPLAY_GUARD',version:'0.3.3',status:collisionBranches?'UPSTREAM_REPLAY_GUARD_ACTIVE':auditedBranches?'PROVENANCE_DIVERSITY_AUDITED':'NO_ADJUDICATION_CONTEXT_TO_AUDIT',unresolved,provenanceDiversity:{auditedBranches,collisionBranches,formatMutationCollisionBranches,contentCollisionBranches,contentFormatMutationCollisionBranches,nearContentCollisionBranches,sourceIndependenceProven:false},closureAuthority:'NONE',boundary:'Deterministic cross-organ provenance/content replay guard v0.3.3 public state. Multiplication/division preservation is present as an unpromoted patch pending dedicated adversarial CI verification; no semantic equivalence, source independence, or truth is inferred.'};}
export {provenanceAudit,canonicalProvenance,canonicalMaterial,boundedNearDuplicate,protectSemanticOperators,semanticOperatorSignature};
