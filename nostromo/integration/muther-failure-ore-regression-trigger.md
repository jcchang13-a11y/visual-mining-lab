# MUTHER failure-ore cross-organ regression trigger

This marker records the first regression pass for `zenomorph-muther-failure-ore/v0.1`.

Capability under test: a ZENOMORPH failure artifact may be mined as active ore only when provenance-bound failure traits participate in a mutating operation and pressure a candidate that also contains non-failure material. Merely inheriting or citing a failure is archival reuse, not metabolism.

Safety boundary: this capability creates sandbox evidence only. It does not authorize incorporation, persistent body mutation, aesthetic promotion, or autonomous deployment.

The file intentionally lives under `nostromo/integration/` so the existing full NOSTROMO integration workflow re-runs after the focused MUTHER CI has passed. DISPLAYED STATE MUST FOLLOW EVIDENCE: do not promote public status unless the integration workflow also succeeds.
